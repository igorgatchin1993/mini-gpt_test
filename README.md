# mini-gpt_test

The model successfully learned character-level Shakespeare-style structure, including speaker names, punctuation, line breaks, and word-like generation, but output quality is limited because the model is small and trained for a short time.
